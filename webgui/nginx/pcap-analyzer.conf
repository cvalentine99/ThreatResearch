server {
    listen 80;
    server_name pcap.local;  # Change to your domain

    # Serve SvelteKit frontend
    location / {
        root /var/www/pcap-analyzer/frontend/build;
        index index.html;
        try_files $uri $uri/ /index.html;
    }

    # Proxy API requests to FastAPI backend
    location /api {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # Increase timeout for large uploads
        proxy_connect_timeout 600;
        proxy_send_timeout 600;
        proxy_read_timeout 600;
        send_timeout 600;

        # Allow large file uploads
        client_max_body_size 10G;
    }

    # Proxy SSE streaming endpoint (requires special headers)
    location /api/v1/stream {
        proxy_pass http://127.0.0.1:8000;

        # SSE specific headers
        proxy_http_version 1.1;
        proxy_set_header Connection '';
        proxy_set_header Cache-Control no-cache;
        proxy_set_header Accept text/event-stream;

        # Critical: disable buffering for SSE
        proxy_buffering off;
        proxy_cache off;

        # Standard proxy headers
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # Long timeout for persistent connections
        proxy_connect_timeout 7d;
        proxy_send_timeout 7d;
        proxy_read_timeout 7d;
    }
}

# HTTPS configuration (for production)
# Uncomment and configure after obtaining SSL certificate
#
# server {
#     listen 443 ssl http2;
#     server_name pcap.yourdomain.com;
#
#     ssl_certificate /etc/letsencrypt/live/pcap.yourdomain.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/pcap.yourdomain.com/privkey.pem;
#
#     # SSL configuration
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers HIGH:!aNULL:!MD5;
#     ssl_prefer_server_ciphers on;
#
#     # Same location blocks as above...
# }
#
# # Redirect HTTP to HTTPS
# server {
#     listen 80;
#     server_name pcap.yourdomain.com;
#     return 301 https://$server_name$request_uri;
# }
